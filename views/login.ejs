<!doctype html>
<html lang="pt-BR">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>
        <%= titulo %>
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
</head>
<!-- 
"d-flex": Aplica a classe d-flex, que é parte do framework CSS Bootstrap. Essa classe transforma o elemento em um container flexível (Flexbox), permitindo o uso de propriedades de layout flexíveis.

flex-column: Configura o layout flexível para organizar os elementos filhos em uma coluna vertical (em vez de uma linha horizontal, que é o padrão do Flexbox).

min-vh-100: Define a altura mínima do elemento como 100% da altura da janela de visualização (viewport height). Isso garante que o <body> ocupe toda a altura da tela, mesmo que o conteúdo seja menor.

justify-content-center: Centraliza os elementos filhos verticalmente dentro do <body>. Essa propriedade é útil para alinhar o conteúdo no centro da página. -->

<body class="d-flex flex-column min-vh-100 justify-content-center">
    <!-- Conteudo da página -->
    <header>

    </header>

    <main class="container w-50 text-center bg-info p-5">
        <!-- Criando formulario de login -->
        <form action="/usuarios/login" method="POST">
            <!-- Caixinha de email -->
            <label for="inputEmail" class="form-label fs-2 mt-2">Email</label>
            <input type="email" id="inputEmail" class="form-control form-control-lg text-center"
                placeholder="Digite seu email..." name="email">
            <!-- Fim da caixinha de email -->
            <!-- Caixinha de Senha -->
            <label for="inputSenha" class="form-label fs-2 mt-2">Senha</label>
            <input type="password" class="form-control form-control-lg text-center" id="inputSenha"
                placeholder="Digite sua senha..." name="senha">
            <!-- Fim da caixinha de Senha -->
            <!-- Botão para enviar os dados -->
            <button type="submit" class="btn btn-primary btn-lg mt-3">Login</button>
        </form>

        <!-- Caixinha de alerta só aparece se ele errar o login-->
        <% if (typeof erro !== "undefined") { %>
         <!-- Caixinha de alerta -->
        <div class="alert alert-danger text-center mx-auto mt-3" style="width: 50%;">
            Email ou senha inválidos
        </div>
        <% } %>
        
    </main>

    <footer>

    </footer>

    <!-- Importação do JavaScript do bootstrap -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI"
        crossorigin="anonymous"></script>
</body>

</html>